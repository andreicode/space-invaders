Assets.register({
    name: 'menu-cursor',
    init: function(options) {
        var cursor = Assets.create('text-label', {
            color: __CONFIG['color-accent'],
            text: '>'
        });

        this.object.add(cursor);
        
    },
    update: function() {
        
    }
});
