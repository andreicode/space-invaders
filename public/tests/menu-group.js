Scene.register({
    name: 'menu-group',
    init: function () {

        var menu = Assets.create('menu-group');
        menu.position.x = -40;
        this.scene.add(menu);

    },
    update: function () {
        
    }
});
